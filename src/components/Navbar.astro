<nav>
  <ul class="nav-links">
    <li><a href="#hero">Home</a></li>
    <li><a href="#projects">Projects</a></li>
    <li><a href="#skills">Skills</a></li>
    <li><a href="#about">About me</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
</nav>

<style>
  .nav-links {
    position: fixed;
    top: 0;
    display: flex;
    justify-content: center;
    gap: 20px;
    list-style-type: none;
    width: 100%;
    left: 50%;
    transform: translateX(-50%);
    padding-block: 1rem;
    box-shadow: 2px 0px 2.5rem #202020b4;
    background: #2e2e2e62;
    backdrop-filter: blur(25px);
    animation: reveal-nav 1.2s ease-out forwards;
    z-index: 20;
  }

  .nav-links a {
    position: relative;
    display: inline-block;
    color: white;
    text-decoration: none;
    font-size: 1rem;
    font-weight: 800;
    text-transform: uppercase;
    /* background: var(--glass-bg); */
    background: linear-gradient(
      to bottom,
      rgba(255, 255, 255, 0.25) 0,
      rgba(117, 117, 117, 0.15) 20%,
      rgba(92, 92, 92, 0.15) 30%,
      rgba(0, 0, 0, 0.3) 70%,
      rgba(41, 41, 41, 0.3) 90%
    );
    padding: 0.5em 1.5em;
    border: 1px solid var(--faint-border);
    box-shadow: 2px 2px 5px #111;
    text-shadow: 0px 0px 5px rgba(59, 59, 59, 0.541);
    transition: 0.2s;
  }

  .nav-links a:hover {
    background: var(--glass-bg-secondary);
    color: #111;
  }
  nav {
    /* margin-block: 1rem 2rem; */
    width: 70%;
    margin: 1.3rem auto 2rem;
  }

  @keyframes reveal-nav {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
</style>
