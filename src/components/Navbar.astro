<nav>
  <ul class="nav-links">
    <li><a href="#hero">Home</a></li>
    <li><a href="#projects">Projects</a></li>
    <li><a href="#skills">Skills</a></li>
    <li><a href="#about">About me</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
</nav>

<style>
  .nav-links {
    position: fixed;
    top: 0;
    display: flex;
    justify-content: center;
    gap: 5px;
    list-style-type: none;
    width: 100%;
    left: 50%;
    transform: translateX(-50%);
    /* padding-block: 1rem; */
    box-shadow: 2px 0px 2.5rem #202020b4;
    background: linear-gradient(
      to bottom,
      rgba(168, 167, 167, 0.25) 0,
      rgba(117, 117, 117, 0.15) 20%,
      rgba(46, 46, 46, 0.3) 90%
    );
    border-bottom: 1px solid black;
    border-top: 2px solid rgb(92, 91, 91);
    backdrop-filter: blur(3px);
    animation: reveal-nav 0.5s ease-out forwards;
    z-index: 20;
    padding-block: 6px;
  }

  .nav-links a {
    position: relative;
    display: inline-block;
    color: white;
    text-decoration: none;
    font-size: 1rem;
    font-weight: 400;
    letter-spacing: 1px;
    padding: 0.5em 1.5em;
    border-radius: 5px;

    background: var(--aero-black);
    text-shadow: 0px 0px 5px rgba(59, 59, 59, 0.541);
    transition: 0.2s;
    padding-block: 1rem;
    /* border-inline: 1px solid rgb(26, 25, 25); */
    border: 1px solid #111;
  }

  .nav-links a:hover {
    background: var(--aero-blue);
  }
  nav {
    /* margin-block: 1rem 2rem; */
    width: 70%;
    margin: 1.3rem auto 2rem;
  }

  @keyframes reveal-nav {
    from {
      transform: translate(-50%, -40px);
    }
    to {
      transform: translate(-50%, 0);
    }
  }
</style>
