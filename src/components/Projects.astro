---
import * as Icons from "simple-icons";
import projects from "../data/projects.json";
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";

// Map tech names to simple-icons keys
const iconMap: Record<string, any> = {
  react: Icons.siReact,
  javascript: Icons.siJavascript,
  "node.js": Icons.siNodedotjs,
  astro: Icons.siAstro,
  typescript: Icons.siTypescript,
  css: Icons.siCss,
};

const imageModules = import.meta.glob<{ default: ImageMetadata }>(
  "../assets/thumbnails/*.{png,jpg,jpeg,webp,avif}",
  { eager: true },
);

const imageMap: Record<string, ImageMetadata> = Object.fromEntries(
  Object.entries(imageModules).map(([path, module]) => {
    const fileName = path.split("/").pop() ?? "";
    const key = fileName.replace(/\.[^.]+$/, "");
    return [key, module.default];
  }),
);
---

<section id="projects" data-reveal>
  <h2 class="section-title">Projects</h2>
  <div class="projects-container">
    {
      projects.projects.map((project) => (
        <div class="card">
          <header>
            <div class="card-media">
              {imageMap[project.thumbnail] ? (
                <Image src={imageMap[project.thumbnail]} alt={project.title} />
              ) : (
                <div class="thumbnail-fallback" aria-hidden="true" />
              )}
            </div>
            <h3>{project.title}</h3>
          </header>
          <main>
            <p class="project-description">
              {project.description.split("\n").map((line) => (
                <span class="desc-line">{line}</span>
              ))}
            </p>
            <ul class="tech-stack">
              {project.tech.map((tech) => {
                const icon = iconMap[tech];
                return icon ? (
                  <li>
                    <span class="tech-pill">
                      <svg
                        role="img"
                        viewBox="0 0 24 24"
                        xmlns="http://www.w3.org/2000/svg"
                        width="16"
                        height="16"
                      >
                        <path d={icon.path} fill="currentColor" />
                      </svg>
                      {icon.title}
                    </span>
                  </li>
                ) : null;
              })}
            </ul>
          </main>
          <footer>
            {project.demo && (
              <a href={project.demo} target="_blank">
                Live demo
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  aria-hidden="true"
                >
                  <path d="M15 3h6v6" />
                  <path d="M10 14 21 3" />
                  <path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6" />
                </svg>
              </a>
            )}
            <a href="">
              View code
              <svg
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
                aria-hidden="true"
              >
                <path d={Icons.siDiscord.path} fill="currentColor" />
              </svg>
            </a>
          </footer>
        </div>
      ))
    }
  </div>
</section>
<style>
  section {
    margin-top: 9vh;
    padding-inline: clamp(0.75rem, 2vw, 1.25rem);
  }

  .projects-container {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1rem;
    align-items: stretch;
  }
  .project-description {
    text-wrap: pretty;
    max-width: 60ch;
    margin: 0.65rem 0 1rem;
    line-height: 1.35;
    font-size: clamp(0.97rem, 1vw, 1.07rem);
    color: rgba(241, 247, 255, 0.92);
  }
  .desc-line {
    display: block;
  }
  .desc-line + .desc-line {
    margin-top: 0.35rem;
  }

  h3 {
    color: #dad9d9cc;
    transition: color 0.2s;
  }
  .card {
    width: 100%;
    min-width: 0;
    min-height: 100%;
    margin: 0 auto;
    border: 1px solid rgba(126, 135, 145, 0.35);
    border-radius: 14px;
    overflow: hidden;
    padding: 0;
    background: #121212;
    box-shadow:
      0 10px 24px rgba(0, 0, 0, 0.28),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
    text-shadow: 0 0 5px rgba(0, 0, 0, 0.65);
    transition: 0.2s;
    display: flex;
    flex-direction: column;
    p,
    .tech-stack {
      margin-left: 0;
    }
    &:hover {
      background: #142d4e;
    }
  }
  .card header {
    h3 {
      border-top: 1px solid rgba(177, 183, 189, 0.3);
      margin: 0;
      padding: 0.85rem 1rem 0.1rem;
      font-size: clamp(1.3rem, 2vw, 1.65rem);
      letter-spacing: 0.01em;
      transition: 0.2s;
    }
  }
  .card-media {
    height: clamp(180px, 24vw, 230px);
    overflow: hidden;
    border-radius: 12px 12px 0 0;

    img {
      display: block;
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.3s ease-out;
    }
  }
  .thumbnail-fallback {
    width: 100%;
    height: 100%;
    background: var(--glass-bg);
    transition: transform 0.3s ease-out;
  }
  .card:hover {
    border: 1px solid var(--faint-border);
  }
  .card:hover .card-media img,
  .card:hover .card-media .thumbnail-fallback {
    transform: scale(1.05);
  }
  .card:hover header h3 {
    background: linear-gradient(
      to bottom,
      #4fa0f7 0%,
      #4893f5 40%,
      #8fd3ff 100%
    );
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    padding-left: 1.5rem;
    text-shadow: none;
  }
  .card:hover .tech-stack li {
    background: var(--aero-blue);
  }

  .tech-stack,
  .card p {
    padding-inline: 1rem;
  }

  .tech-stack {
    display: flex;
    list-style-type: none;
    justify-content: start;
    gap: 5px;
    margin-block: 24px;
    flex-wrap: wrap;

    li {
      border: 1px solid #dad9d97a;
      padding: 4px 12px;
      border-radius: 25px;
      background: var(--aero-black);
      font-weight: 400;
      font-size: 0.86rem;
      display: flex;
      align-items: center;

      box-shadow: 0 3px 12px rgba(0, 0, 0, 0.55);
    }
  }
  .tech-pill {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 6px;

    svg {
      color: currentColor;
    }
  }
  .card footer {
    margin-top: auto;
    display: flex;
    justify-content: space-around;
    align-items: center;
    border-top: 1px solid rgba(178, 187, 199, 0.22);

    a {
      position: relative;
      color: white;
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      border: 1px solid black;
      padding-block: 0.85rem;
      border-radius: 0 0 5px 5px;
      background: var(--aero-black);
      z-index: 1;
      letter-spacing: 0.01em;
      font-weight: 500;

      &::after {
        content: "";
        position: absolute;
        inset: 0;
        border-radius: 0 0 5px 5px;
        z-index: -1;
        background: var(--aero-blue);
        opacity: 0;
        transition: opacity 0.2s ease-out;
      }

      &:hover::after {
        opacity: 1;
      }
    }

    svg {
      height: 16px;
      aspect-ratio: 1;
      margin-left: 0;
    }
  }

  @media (max-width: 980px) {
    .projects-container {
      grid-template-columns: 1fr;
      gap: 0.9rem;
    }

    .card-media {
      height: clamp(180px, 42vw, 240px);
    }
  }

  @media (max-width: 640px) {
    section {
      margin-top: 8vh;
      padding-inline: 0.35rem;
    }

    .card header h3 {
      padding: 0.7rem 0.75rem 0.1rem;
    }

    .project-description {
      margin: 0.55rem 0.75rem 0.9rem;
      font-size: 0.95rem;
    }

    .tech-stack {
      margin: 0.85rem 0.75rem 1rem;
    }

    .card footer a {
      font-size: 0.9rem;
      padding-block: 0.72rem;
    }
  }
</style>
