---
import Cta from "../components/Cta.astro";
---

<section id="hero">
  <header>
    <h1>Mirko Joel Ruhl</h1>
    <h2>Full Stack Developer</h2>
  </header>
  <main data-reveal>
    <div class="hero-info">
      <div class="achievements">
        <p><span>4+</span><br />Years experience</p>
        <p><span>User</span><br />Experience first</p>
        <p><span>Scalable</span><br />Projects</p>
      </div>

      <p>Frontend ◦ Backend ◦ Designer. <br />Building friendly UX</p>

      <div class="hero-cta">
        <a class="cta" href="#projects">View Projects</a>
        <a href="/resume.pdf" download>Download resume</a>
        <a href="#contact">Contact</a>
      </div>
    </div>
    <Cta />
  </main>
</section>

<style>
  body {
    font-family: inherit;
  }
  h1 {
    transition: 0.3s;
    width: fit-content;
    white-space: nowrap;
    margin: 0 auto;
    margin-bottom: 0.75rem;
    letter-spacing: 0.12em;
  }

  h2 {
    font-weight: 400;
    letter-spacing: 0.18em;
    font-size: clamp(1.05rem, 1.8vw, 1.35rem);
    width: fit-content;
    color: var(--text-primary);
    /* background: linear-gradient(
      180deg,
      rgba(18, 18, 18, 0.92),
      rgba(10, 16, 28, 0.96)
    ); */
    background: var(--aero-black);
    padding: 0.45rem 1.5rem;
    margin: 0 auto 1.25rem;
    border: 1px solid rgba(187, 197, 210, 0.32);
    border-radius: 999px;
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.35);
  }

  #hero header {
    animation: load-up 700ms ease-out both;
  }

  .hero-info {
    a {
      border: 1px solid rgb(133, 133, 133);
      color: white;
      padding: 1rem;
      text-decoration: none;
      border-radius: 15px;
      background: var(--aero-black);
      min-width: 120px;
      text-align: center;

      &:hover {
        background: var(--aero-blue);
      }
    }

    .cta {
      background: radial-gradient(rgb(0, 112, 216), rgb(0, 39, 168));
      /* background: var(--aero-blue); */
    }
  }

  .hero-cta {
    display: flex;
    justify-content: start;
    gap: 8px;
    margin-top: 1.4rem;
  }

  .achievements {
    display: flex;
    justify-content: space-between;
    gap: 5px;
    margin-bottom: 1.25rem;
    font-family: "Jost";

    p {
      border: 1px solid rgb(80, 80, 80);
      height: 120px;
      background: #202020;
      padding: 1.7rem 1rem 0;
      /* box-shadow: 0 5px 10px black; */
      box-shadow: 0 12px 18px rgba(0, 0, 0, 0.55);
      span {
        font-size: 2rem;
        margin: 6px 0;
      }
    }
  }

  .hero-info > p {
    max-width: 26ch;
    font-size: clamp(1.05rem, 1.5vw, 1.2rem);
    line-height: 1.7;
    color: var(--text-primary);
  }

  section {
    padding-block: 6vh;

    margin-bottom: 0;
  }

  main {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 5rem;
    align-items: stretch;
    margin-top: 7rem;
  }

  @media (max-width: 980px) {
    main {
      grid-template-columns: 1fr;
      gap: 0.9rem;
    }
    .hero-info {
      margin: 0;
    }
    .achievements {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 1rem;
      width: 100%;
    }
  }

  #hero {
    margin: 20vh auto 10vh;
  }
  @keyframes load-up {
    0% {
      opacity: 0;
      transform: translateY(28px) scale(0.92);
    }
    100% {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }
</style>
